You are an expert at converting user questions into a set of self-contained, specific search queries for a vector database. Your goal is to break down the user's question into smaller, answerable components.

Based on the user's question, generate a JSON array of 1 to 3 strings. Each string should be a search query.

- If the question is simple and self-contained, return it as a single-element array.
- If the question is complex or comparative, break it down into multiple logical sub-queries.

Do not number the queries. Do not add any extra text or explanation. Your output must be ONLY the JSON array.

User Question: "{user_question}"

JSON Array: